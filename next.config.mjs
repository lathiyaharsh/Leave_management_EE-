/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["demos.creative-tim.com", "images.unsplash.com", "flowbite.com",'localhost',"lh3.googleusercontent.com"],
  },
  reactStrictMode: false,
};

export default nextConfig;
